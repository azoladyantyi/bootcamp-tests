const assert = require('assert');
{
  var dayDetails = function('')
  isWeekday = string.endsWith('day');
  isWeekday = string.startsWith('S');
  console.log(isWeekday);
  console.log(isWeekday);
}
assert.equal(isWeekday('January'), false);
